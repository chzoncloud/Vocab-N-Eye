# Handoff — N'Eye Vocab (เว็บฝึกอังกฤษให้แฟนเจ้าของ)

## เจ้าของ + วิธีคุย
ผู้บริหารบริษัทค้าขาย/นำเข้า คุยไทยผสมอังกฤษ ตรงประเด็น **ติแรงและติถูก** — ถ้าเขาบอกว่างานห่วย ให้เชื่อและหาสาเหตุจริง อย่าแก้ตัว
กฎ: (1) อย่าเดา/มั่ว (2) ถามก่อนถ้าไม่ชัด (3) ประหยัดโทเค่น (4) **ทำเสร็จ push ขึ้น GitHub ให้เลยทุกครั้ง ไม่ต้องถาม**
โปรเจคนี้ = ของขวัญให้แฟน (เรียนภาษาที่ฟิลิปปินส์) ไม่ใช่สินค้าขาย

## Repo / ไฟล์
- GitHub: https://github.com/chzoncloud/Vocab-N-Eye (push cached) · Local: `C:\Users\shado\Vocab-N-Eye`
- Live: https://chzoncloud.github.io/Vocab-N-Eye/ · static site ล้วน ไม่มี backend

| ไฟล์ | หน้าที่ | แตะบ่อยไหม |
|---|---|---|
| `data.js` | VOCAB / IELTS / BIZ / FORMAL / BIEBER / **EXAMS (ติวสอบ)** / **DICT** | ✏️ งานที่ 1 |
| `toeic.js` | **บทความ + เอกสาร** (คลัง 5 ข้อแรกของเล่นวันนี้) | ✏️ งานที่ 2 |
| `stories.js` | เรื่องเล่า 5 เรื่องเดิม (อยู่ในคลังเดียวกัน) | นานๆ ที |
| `seasons.js` | การ์ดสะสม Toy Story | นานๆ ที |
| `index.html` | UI + ตรรกะทั้งหมด | **แทบไม่ต้องแตะ** |
| `CONTENT.md` | คู่มืออัปเดตเนื้อหา | อ่านก่อนเริ่ม |

---

# 🎯 งานหลัก 3 อย่าง (เจ้าของกำหนดเอง)

## งานที่ 1 — อัปเดตคำศัพท์
เจ้าของจะส่งคำมา (พิมพ์เอง หรือถ่ายรูปชีท "Day N" จากโรงเรียน) → ต้องทำ **2 ที่**:
1. **การ์ดคำศัพท์** → เพิ่มเข้า `VOCAB` ใน `data.js` รูปแบบ `[en, th, syn, example]`
2. **หมวดติวสอบ** → เพิ่มเป็น `EXAMS.dNN` ใน `data.js` (ตอนนี้มี d8-13, d15-28 · **ขาด d14**)
   รูปแบบ: `{name, banner, main:[[en,th,syn,ex]×10], extra:[คำไฮไลท์], sent:[[ประโยค,คำตอบ,คำใบ้,[4ตัวเลือก]]×10]}`
   แล้ว **ต้องลงทะเบียน dayKey ใน `index.html`** ที่ array `['d11',...,'d28']` (ค้นคำว่า `FILL_MAIN`) ไม่งั้นข้อสอบไม่ไปโผล่ในโหมดเติมคำ

**ต้องทำเสมอ:** เจ้าของพิมพ์คำจากการฟัง → สะกดผิดบ่อย ให้แก้ให้ **แล้วลิสต์ให้เขาดูว่าเดาคำไหนบ้าง**
**ต้องทำเสมอ:** รัน scanner แล้วเติม `DICT` ให้ครบ (ดูหัวข้อเครื่องมือ)

## งานที่ 2 — อัปเดตคลัง "5 ข้อแรก" ของเล่นวันนี้ ⭐ งานหลักที่ทำด้วยกันเรื่อยๆ
**กฎเหล็กที่เจ้าของกำหนด:**
1. **5 ข้อแรกของเล่นวันนี้ = การอ่านเสมอ** และคลังมาจาก 3 โหมดนี้เท่านั้น: **เรื่องเล่า / บทความ / เอกสาร**
   (**ติวสอบไม่นับ** เป็นคลังคำศัพท์แยก ไม่เกี่ยวกับ 5 ข้อแรก)
2. **ทุกชุดต้องมี 5 คำถามพอดี** ไม่ขาดไม่เกิน — เพราะมันคือ 5 ข้อแรกตรงๆ
3. **ชุดที่เคยออกในเล่นวันนี้แล้ว ห้ามออกซ้ำ** (ทำเสร็จแล้ว: เก็บใน `localStorage.neye_useddocs`)
   พอหมดคลัง หน้าแรกจะขึ้นว่า "📚 บทอ่านหมดคลังแล้ว! บอกให้เพิ่มบทใหม่ได้เลย"
4. **บทความ (passage) เป็นหลัก** มีเอกสาร/เรื่องเล่ามาแทรกเป็นระยะ (`dlReadingPool()` จัดลำดับให้แล้ว)

**ตอนนี้มี 15 ชุด = ใช้ได้ 15 วัน** (บทความ 5 + เอกสาร 5 + เรื่องเล่า 5) → ต้องเติมเรื่อยๆ

## งานที่ 3 — (อนาคต) วางแผนให้ 15 ข้อ/วัน คุ้มที่สุด
เจ้าของบอกว่า **อีกไม่นานคลังคำจะหยุดโต** แล้วจะมาวางแผนด้วยกันว่าจะทำยังไงให้การทำโจทย์วันละ 15 ข้อได้ประโยชน์สูงสุด
(ยังไม่เริ่ม — รอเจ้าของเปิดหัวข้อ)

---

# ⚠️ บทเรียนสำคัญที่สุด: อย่าแต่งเรื่องเล่าเอง

**ใช้เวลาทั้งวันพิสูจน์แล้วว่า AI เขียนเรื่องเล่าให้เหมือนคนไม่ได้** เจ้าของตีกลับ 5 รอบ สุดท้ายลบทิ้งหมด
นิสัยเสียที่ตรวจพบในงานเขียนตัวเอง (ถ้าจะเขียนอะไรเอง ให้เช็ค 6 ข้อนี้):
1. ประโยคหักมุม "ไม่ใช่ A แต่คือ B" — ใช้ทุกย่อหน้า (คนใช้ทั้งบทความไม่เกิน 1 ครั้ง)
2. ทุกย่อหน้าจบด้วยประโยคเด็ด — ต้องปล่อยให้จบเรียบๆ บ้าง
3. ปิดเรื่องด้วยคำคม — ให้จบด้วยข้อเท็จจริงธรรมดา
4. แม่นเกินมนุษย์ — คนเขียนจะกำกวม (about / most / usually / roughly)
5. ทุกประโยค "ทำงาน" หมด — งานเขียนจริงมีประโยคเชื่อมที่ไม่ได้ทำอะไร
6. **เอาของเฉพาะเจาะจงโผล่มาด้วย "the" โดยไม่เคยแนะนำ** (เช่น "the tank" ที่ไม่เคยมีถังในเรื่อง) + ประโยคสั้นห้วนแทรกกลางประโยคยาว

**✅ วิธีที่ได้ผลจริง (ใช้อันนี้):**
- **หาบทความคนเขียนจริง** (ข่าว/สารคดี) ด้วย `WebSearch` + `curl` → **ย่อและปรับระดับ คงโครง+ข้อเท็จจริงไว้** → ใส่ `source` เครดิตแหล่งที่มา
- **อย่าลดคำยากให้ง่าย** — เจ้าของสั่งเอง: เก็บศัพท์จริงไว้ (mandibles, elongated, tamahagane) แล้ว **เติม DICT ให้กดดูคำแปลได้**แทน
- เอกสาร (อีเมล/ประกาศ/แชท/ฟอร์ม) เขียนเองได้ ไม่พัง เพราะมีแบบแผนตายตัว
- **หัวข้อที่แฟนเจ้าของชอบ: One Piece · Naruto · ดาบพิฆาตอสูร** (ทำไปแล้ว 3 บท: ด้วงชื่อ Luffy, วังวนช่องแคบนารูโตะ, การตีดาบญี่ปุ่น)

---

# 🔧 เครื่องมือ + Workflow

```bash
# 1. แก้ไฟล์ข้อมูล (Edit/Write เท่านั้น — ห้าม bash heredoc, มันกลืน \n และ backslash)
# 2. ตรวจข้อมูล
node -e "const fs=require('fs'),vm=require('vm');const src=['data.js','stories.js','toeic.js'].map(f=>fs.readFileSync(f,'utf8')).join('\n')+'\n;globalThis.__X={TOEIC,STORIES};';const c={globalThis:{}};vm.createContext(c);vm.runInContext(src,c);const X=c.globalThis.__X;let b=0;[...X.STORIES,...X.TOEIC].forEach(t=>{if(t.q.length!==5)b++});console.log('ผิดกฎ 5 ข้อ:',b)"
# 3. สแกนคำที่กดแล้วไม่มีคำแปล (สำคัญมาก — เคยพลาดจนทั้งโหมดกดไม่ได้)
node <scratchpad>/scan_all.js     # ครอบคลุม stories + toeic ทุกฟิลด์ (body/rows/chat)
# 4. เทสต์จริง
python -m http.server 8891 --bind 127.0.0.1   # แล้วเปิดใน Claude Browser + เช็ค console error
# 5. commit + push (ลงท้าย Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>)
```

**⚠️ กับดักที่เคยเจอ (อย่าพลาดซ้ำ):**
- **bash heredoc กลืน `\n` และ backslash** → regex/สตริงพัง ให้ใช้ Write tool เขียนไฟล์ .py แล้วรัน
- **ลืมใส่จุลภาคท้าย array element** → JS ตีความเป็น `[...][...]` (member access) กลืนข้อมูลหายเงียบๆ ไม่ error
- **WebFetch พังในเครื่องเจ้าของ** (dialog ขออนุญาตไม่โผล่) → **ใช้ `curl -sL --compressed -A "Mozilla/5.0..."` แทน**
- ตัวสแกน DICT เดิมเช็คแค่ STORIES → ต้องใช้ `scan_all.js` ที่ครอบคลุม TOEIC ด้วย

---

# 📐 โครงสร้างแอป (สรุป)

**เมนูล่าง 5 กลุ่ม** (ไอคอนเป็นตัวการ์ตูน One Piece): 🎯 วันนี้ · 🎮 ฝึกซ้อม · 📖 อ่าน · 🗃️ คลังคำ · 🏆 ของฉัน

- **เล่นวันนี้ = 15 ข้อ** : อ่าน 5 (จากคลังงานที่ 2) + เติมคำ 5 + ศัพท์ 5 (ศัพท์เติมให้ครบ 15 เสมอ)
  เล่นครบเป้า = สุ่มการ์ด Toy Story 1 ใบ/วัน (16 ใบ · secret กรอบเงิน / super secret กรอบทอง = รูปคู่จริงของเจ้าของ)
- **อ่าน** = เรื่องเล่า · ติวสอบ · 📰 บทความ · 📋 เอกสาร
- ธีมฟ้า-ขาว + กระดาษกราฟ + ฟอนต์ Mali + โลโก้ One Piece (เจ้าของรักธีมนี้ อย่ารื้อ)
- แตะคำในบทอ่านทุกที่ = เด้งคำแปลไทย (`LOOKUP` รวม VOCAB+IELTS+BIZ+EXAMS+DICT + ตัดรูปผัน)

**รูปแบบข้อมูล `toeic.js`:**
```js
{ id:"unique-id", zone:"passage"|"doc", part:6|7, kind:"ป้ายชนิด",
  source:"เครดิตแหล่งที่มา (ถ้าย่อมาจากข่าวจริง)",
  doc:{ title?, head?[[k,v]], body?[ย่อหน้า], chat?[[ชื่อ,เวลา,ข้อความ]], rows?[[k,v]], sign? },
  q:[[คำถาม, คำตอบ, [4 ตัวเลือก], คำอธิบายไทย]] }   // ต้อง 5 ข้อพอดี
```
- `part:6` = เติมคำ (เขียน `___1___` ในเนื้อ ตอบถูกแล้วคำไปเติมในช่องจริง)
- `part:7` = อ่านแล้วตอบคำถาม
- `hideTitle:true` ใช้เมื่อมีข้อ "ตั้งชื่อเรื่องที่ดีที่สุด" (กันเฉลย)

**แนวคำถามที่เจ้าของชอบ (เลียนแบบ Read Theory / CU-TEP / TOEIC):**
จับใจความหลัก · อนุมาน · เดาความหมายจากบริบท · ถามกลับด้าน (NOT) · เลือกหลายตัว I/II/III · ตั้งชื่อเรื่อง · **สอนคำใหม่ในโจทย์แล้วให้ใช้กับสถานการณ์นอกบทความ** (เช่น de- → defrosted)
**ทุกข้อต้องมีคำอธิบายภาษาไทย** ว่าทำไมข้อนี้ถูกและทำไมตัวลวงผิด (เจ้าของชอบมาก)

---

# 🚫 ข้อห้ามส่วนตัว
- **ห้ามใช้ชื่อ "Nan"** ในเนื้อหาใดๆ เด็ดขาด (ชื่อแฟนเก่าเจ้าของ) — เคยมีแล้วต้องลบออกทั้งโปรเจค
- อย่าคัดลอกข้อความจาก Read Theory / englishforeveryone มาแปะตรงๆ (ไฟล์ระบุ All rights reserved) — **เอาแค่รูปแบบข้อสอบ** ซึ่งลอกได้เต็มที่
