# 📒 CONTENT.md — คู่มืออัปเดตเนื้อหา (อ่านแค่ไฟล์นี้พอ)

> เป้าหมาย: ต่อจากนี้อัปเดตแค่ **2 อย่าง** = เนื้อเรื่อง (`stories.js`) + รางวัล/ซีซั่น (`seasons.js`)
> **ไม่ต้องเปิด `index.html` หรือ `data.js`** เพื่อประหยัด context
> เสร็จแล้ว `git add -A && git commit && git push` ได้เลย

---

## 📖 1) เพิ่มเรื่องเล่า → แก้ `stories.js` ไฟล์เดียว

โครงสร้าง 1 เรื่อง:
```js
{ t:"ชื่อเรื่อง (อังกฤษ)",
  text:"เนื้อเรื่องภาษาอังกฤษ ... (แตะคำดูคำแปลได้อัตโนมัติ)",
  q:[
    ["คำถาม 1?","คำตอบถูก",["คำตอบถูก","ตัวลวง","ตัวลวง","ตัวลวง"]],
    ["คำถาม 2?","คำตอบถูก",["ตัวเลือก1","ตัวเลือก2","ตัวเลือก3","ตัวเลือก4"]],
    ... รวม 5 ข้อ
  ]
}
```
**กติกา**
- แต่ละเรื่องมี **5 คำถาม** พอดี (โหมด "เล่นวันนี้" ดึง 5 ข้อ)
- เรื่องเล่า **เรียงลำดับ 1→2→3…** (เป็นหลักสูตรไล่ระดับความยาก) — เพิ่มเรื่องใหม่ต่อท้าย array
- คำตอบถูกต้องอยู่ใน array ตัวเลือกด้วยเสมอ
- คำในเนื้อเรื่องแตะดูคำแปลได้เอง (ระบบดึงจาก DICT/คลังคำ) — ไม่ต้องทำอะไรเพิ่ม

---

## 🏆 2) เพิ่ม/แก้ซีซั่นการ์ดสะสม → แก้ `seasons.js` + วางรูป

### เพิ่มการ์ดในซีซั่นเดิม / แก้ชื่อ-ความหายาก
ในแต่ละการ์ด: `{ n:เลขรูป, name:"ชื่อ", rarity:"..." }`
- `n` = ชื่อไฟล์รูป (เช่น `n:3` → `assets/seasons/<dir>/3.png`)
- `rarity` = `common` (ออกง่าย) · `secret` (หายาก) · `supersecret` (หายากสุด)
- น้ำหนักการสุ่มอยู่ที่ `RARITY` (common:10 / secret:3 / supersecret:1) — ยิ่งเลขน้อยยิ่งออกยาก

### เพิ่มซีซั่นใหม่ทั้งชุด (เช่นซีซั่น 2)
1. สร้างโฟลเดอร์รูป: `assets/seasons/<ชื่อซีซั่น>/` แล้ววางรูป `1.png, 2.png, …`
   - รูป **PNG พื้นหลังโปร่ง** สี่เหลี่ยมจัตุรัส 1 ตัว/ไฟล์
2. เพิ่ม object ใหม่ต่อท้าย array `SEASONS`:
```js
{
  id:"season2",                 // ห้ามซ้ำกับซีซั่นอื่น
  name:"ชื่อซีซั่น",
  dir:"assets/seasons/season2/",
  cards:[
    { n:1, name:"...", rarity:"common" },
    ...
  ]
}
```
**กติกาสะสม (อัตโนมัติ ไม่ต้องแก้โค้ด):** เล่น "เล่นวันนี้" ครบเป้า (15 ข้อ) วันละครั้ง = สุ่มได้การ์ด 1 ใบจากตัวที่ยังไม่มี · เก็บครบซีซั่นแล้วระบบเลื่อนไปซีซั่นถัดไปเอง

---

## 🗂️ ไฟล์ในโปรเจค (อ้างอิง)
| ไฟล์ | หน้าที่ | ต้องแตะบ่อยไหม |
|------|--------|----------------|
| `stories.js` | เนื้อเรื่อง | ✏️ อัปบ่อย |
| `seasons.js` | รางวัล/ซีซั่น | ✏️ อัปบ่อย |
| `assets/seasons/**` | รูปการ์ด | ✏️ เวลาเพิ่มซีซั่น |
| `data.js` | คลังคำศัพท์ | นานๆ ที |
| `index.html` | ตัวเครื่อง (UI+ตรรกะ) | แทบไม่ต้องแตะ |
